# \${unit.fileName}.service generated by @project.artifactId@ v@project.version@

[Unit]
Description=\${unit.description}
After=\${unit.after}

[Service]
EnvironmentFile=\${environment.fileName}
WorkingDirectory=\${install.directory}
Type=\${unit.type}
ExecStart=\${install.directory}/\${run.fileName}
Restart=\${unit.restart}
User=\${unit.user}

[Install]
WantedBy=${unit.wantedBy}
